name: Muhammad Ahmad

on:
  push:
    branches: [ main ]

jobs:
  disable:
    runs-on: ubuntu-latest

    steps:
      - name: disable-workflow
        run: echo "This is disabled workflow that i have done"


    
